# Finish the General Conference talk finder (cite2link.gc)
kind: todo
tags: gc
created: 2026-07-25T17:28Z

- 2026-07-25T17:28Z Current state: parses citations like "april2006 wood:instruments" via
gc_cit_pat in cite.py (parse() tries it first), and gc.py::find_talk scrapes
DuckDuckGo HTML for the churchofjesuschrist.org talk URL. Unfinished + inert:
- Not wired into the CLI (app.py only resolves scripture refs); resolve()
  can't turn a GC citation into a Book, so it dead-ends.
- gc.py::find_talk lazy-imports `requests` (an opt-in [gc] extra), returns raw
  HTML, does not extract the actual talk URL, and has no live test
  (gc_test.py::xtest_find_talk is disabled).
- Scraping a search engine is fragile and likely already broken.

To finish: replace the scrape with a real lookup (e.g. the Church's own search
/ content API if one exists), extract the canonical talk URL, add a make_* link
style + wire GC citations through resolve()/print_all, enable the test, and
document the [gc] extra. Until then the GC parse branch is gated off by default
so it can't shadow scripture parsing.
